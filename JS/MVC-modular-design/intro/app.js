// "use strict";

// stari pristup

(function (calcModule, labelModule) {
  var result = calcModule.sum(2, 5);
  var result2 = labelModule.getResult();
  console.log(result2 + result);
})(module1, module2);
